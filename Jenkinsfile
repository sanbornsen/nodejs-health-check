#!/usr/bin/groovy

@Library('github.com/chmouel/osio-pipeline-helpers@master')
def setupScript = null

main {
  checkout scm
  // stage("Build") {
  //   sh "npm install"
  // }
  // stage("Deploy") {
  //   sh "npm run openshift"
  // }
}
